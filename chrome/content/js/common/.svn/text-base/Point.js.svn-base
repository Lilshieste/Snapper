/////////////////////////////////////////////////////////////////////////////// FILENAME:    Point.js// DATE:        9/15/2008// AUTHOR:      Joe Miller//// DESCRIPTION: Defines a structure that stores information about a point in space.//// REFERENCES:  None./////////////////////////////////////////////////////////////////////////////LSDev.Common.Javascript.Point = function( posX, posY ) {    const DEFAULT_X = 0;    const DEFAULT_Y = 0;        var x;    var y;        // Gets the coordinate on the x-axis.    //    // RETURNS Number    this.getX = function() {        return parseInt( x );    }        // Sets the coordinate on the x-axis.    //    // PARAMS    // newX (Number): The new coordinate on the x-axis.    //    // RETURNS void    this.setX = function( newX ) {        if( newX == null ) {            x = DEFAULT_X;        }        else {            x = newX;        }    }        // Gets the coordinate on the x-axis.    //    // RETURNS Number    this.getY = function() {        return parseInt( y );    }        // Sets the coordinate on the y-axis.    //    // PARAMS    // newY (Number): The new coordinate on the y-axis.    //    // RETURNS void    this.setY = function( newY ) {        if( newY == null ) {            y = DEFAULT_Y;        }        else {            y = newY;        }    }        this.setX( posX );    this.setY( posY );}