/////////////////////////////////////////////////////////////////////////////
// FILENAME:    SnappedImage.js                                                 
// DATE:        9/13/2008                                                  
// AUTHOR:      Joe Miller                                                 
//                                                                         
// DESCRIPTION: Defines a set of methods used to access a snapped image.
//                                                                         
// REFERENCES:  None.
/////////////////////////////////////////////////////////////////////////////
LSDev.Extensions.SnapperNS.SnappedImage = {

    // Gets a Javascript Date object representing the date at which the image was snapped.
    //
    // RETURNS Date
    getTimestamp: function() {
        throw new Error( "SnappedImage.getTimestamp is not defined for: " + this );
    },
    
    // Gets a SelectorInfo object representing the selector used to snap the image.
    //
    // RETURNS SelectorInfo
    getSelectorInfo: function() {
        throw new Error( "SnappedImage.getSelectorInfo is not defined for: " + this );
    },
    
    // Gets a string that identifies the location from which the image was snapped (e.g., URL).
    //
    // RETURNS String
    getOrigin: function() {
        throw new Error( "SnappedImage.getOrigin is not defined for: " + this );
    },
    
    // Persists the snapped image to an image file according to the specified arguments,
    //  and returns the full path of the created file.
    //
    // PARAMS
    // path (String): Specifies the directory path in which file should be created.
    // filename (String): Specifies the name of the image file (should NOT include extension).
    // imageType (SnappedImageType): Specifies the format of the image file.
    //
    // RETURNS String
    toImageFile: function( path, filename, imageType ) {
        throw new Error( "SnappedImage.toImageFile is not defined for: " + this );
    },
    
    // Persists the snapped image to a data scheme URI, according to the specified
    //  arguments, and returns the data URI.
    //
    // PARAMS
    // imageType (SnappedImageType): Specifies the image type being represented.
    //
    // RETURNS String
    toDataUrl: function( imageType ) {
        throw new Error( "SnappedImage.toDataUrl is not defined for: " + this );
    }   
}
